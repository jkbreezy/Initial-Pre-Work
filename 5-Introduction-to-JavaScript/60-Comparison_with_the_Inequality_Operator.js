/*We learned about the counterpart to equal: not equal !=, it will also atempt to convert data types to resolve the line of code*/
// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(10);
