/*here we learned that we can simply use return for the value of an if else statement if we are simply looking for a true/false result*/
function isLess(a, b) {
  // Fix this code
  return a < b
}

// Change these values to test
isLess(10, 15);
