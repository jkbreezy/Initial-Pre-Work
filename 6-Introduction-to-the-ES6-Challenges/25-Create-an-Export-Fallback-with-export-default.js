/*This may take another example to nail in solid but this exercise is talking about exporting a default from a file. It infers that when you export default it sets a default value as a "fallback" and this can only be done for one item in a file or module*/
"use strict";
export default function subtract(x,y) {return x - y;}
